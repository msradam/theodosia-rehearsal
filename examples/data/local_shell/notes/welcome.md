# Sandbox

Sample files in this dir get copied into a temp sandbox per MCP session.
Try `ls -la`, `cat data.csv`, `wc -l notes/welcome.md`, or write a new
file under `notes/`. Nothing you do here touches the shipped copy.
