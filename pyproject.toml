[project]
name = "theodosia-rehearsal"
version = "0.1.0"
description = "Ten Theodosia agent scenarios wired as an .mcp.json drop-in for Claude Code."
requires-python = ">=3.11,<3.14"
dependencies = [
    "theodosia[observability]>=0.4.0",
    "pydantic>=2",
    "bandit>=1.8",
    "detect-secrets>=1.5",
    "psutil>=5.9",
]

[tool.uv]
package = false
